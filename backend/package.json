{
  "name": "aim-duel-backend",
  "version": "1.1.0",
  "private": true,
  "description": "Express + Socket.IO runtime for Aim Duel (in-memory, no external services)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.19.2",
    "prom-client": "^15.1.3",
    "socket.io": "^4.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
